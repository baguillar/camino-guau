{
  "framework": "nextjs",
  "rootDirectory": "app",
  "installCommand": "npx prisma generate && npm run build",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 30
    }
  }
}